strict-peer-dependencies = false

# Disable optional dependencies that require native compilation
optional=false

# Skip building native modules in production
npm_config_build_from_source=false

# Use fallback for packages that can't build native modules
npm_config_fallback_to_build=false
